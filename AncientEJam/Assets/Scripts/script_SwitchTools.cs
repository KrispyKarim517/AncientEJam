using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
    AUTHOR: Nichole Wong
    UNITY VERSION: 2020.1.0f1
    LAST MODIFIED: 8/22/2020
    
    A script that allows the user to cycle through tools upon pressing a button
    By default, the cycling button is TAB
*/
public class script_SwitchTools : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
